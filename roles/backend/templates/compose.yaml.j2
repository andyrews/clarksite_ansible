services:
  {{ container_name }}:
    image: {{ image_name }}
    restart: always
    container_name: {{ container_name }}
    networks:
      - {{ docker_network }}
    ports:
      - {{ container_port }}:{{ container_port }}
networks:
  {{ docker_network }}:
    external: true
